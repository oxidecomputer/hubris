[package]
name = "dumptruck"
version = "0.1.0"
edition = "2021"

[dependencies]
humpty = { workspace = true }

userlib = { path = "../../sys/userlib" }
ringbuf = { path = "../ringbuf" }
dump-agent-api = { path = "../../task/dump-agent-api"}

[build-dependencies]
build-util = { path = "../../build/util" }
anyhow = { workspace = true }
serde = { workspace = true }

[lib]
test = false
doctest = false
bench = false
