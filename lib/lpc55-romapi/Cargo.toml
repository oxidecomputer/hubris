[package]
name = "lpc55_romapi"
version = "0.1.0"
edition = "2018"

[features]
default = ["panic-messages"]
panic-messages = []
log-itm = []
log-semihosting = []

[dependencies]
lpc55-pac = "0.4"
num-derive = "0.3.3"
num-traits = { version = "0.2", default-features = false }
cfg-if = "1"

[lib]
test = false
bench = false
