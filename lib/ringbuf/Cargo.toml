[package]
name = "ringbuf"
version = "0.2.0"
edition = "2021"

[features]
# To disable a ring buffer (but leave it otherwise present), enable the
# "disabled" feature
disabled = []

[dependencies]
static-cell = { path = "../static-cell" }

[lib]
test = false
doctest = false
bench = false
