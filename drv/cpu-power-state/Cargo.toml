[package]
name = "drv-cpu-power-state"
version = "0.1.0"
edition = "2021"

[dependencies]
userlib = { path = "../../sys/userlib" }
zerocopy = { workspace = true }
zerocopy-derive = { workspace = true }
num-traits = { workspace = true }
counters = { path = "../../lib/counters", features = ["derive"] }

[lib]
test = false
doctest = false
bench = false

[lints]
workspace = true
