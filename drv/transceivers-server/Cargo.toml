[package]
name = "drv-transceivers-server"
version = "0.1.0"
authors = ["Aaron Hartwig <aaron@oxide.computer>"]
edition = "2021"

[dependencies]
userlib = {path = "../../sys/userlib", features = ["panic-messages"]}
drv-sidecar-front-io = {path = "../sidecar-front-io", features = ["transceivers"]}
drv-transceivers-api = {path = "../transceivers-api", default-features = false}

idol-runtime = {git = "https://github.com/oxidecomputer/idolatry.git"}

num-traits = {version = "0.2.12", default-features = false}
stm32h7 = {version = "0.14", default-features = false}
zerocopy = "0.6.1"

[build-dependencies]
build-util = {path = "../../build/util"}
idol = {git = "https://github.com/oxidecomputer/idolatry.git"}

# This section is here to discourage RLS/rust-analyzer from doing test builds,
# since test builds don't work for cross compilation.
[[bin]]
name = "drv-transceivers-server"
test = false
bench = false