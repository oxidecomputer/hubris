[package]
name = "vsc7448"
version = "0.1.0"
edition = "2018"

[dependencies]
cfg-if = "0.1.10"
userlib = {path = "../../sys/userlib" }

drv-spi-api = {path = "../../drv/spi-api"}
drv-stm32xx-sys-api = {path = "../stm32xx-sys-api", default-features = false, features = ["h743"]}
ringbuf = {path = "../../lib/ringbuf" }
task-net-api = {path = "../../task/net-api" }
vsc-err = { path = "../vsc-err" }
vsc7448-pac = { git = "https://github.com/oxidecomputer/vsc7448" }
vsc85xx = { path = "../vsc85xx" }

[build-dependencies]
build-util = {path = "../../build/util"}

# This section is here to discourage RLS/rust-analyzer from doing test builds,
# since test builds don't work for cross compilation.
[lib]
test = false
bench = false
